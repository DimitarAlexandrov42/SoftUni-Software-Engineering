function stringLength(text, text1, text2) {

    let totalLength = text.length + text1.length + text2.length
    let avaregeLength = Math.floor(totalLength / 3)
    console.log(totalLength);
    console.log(avaregeLength);


}
stringLength('chocolate', 'ice cream', 'cake')